{
  "name": "@sfsr/ui",
  "version": "0.1.0",
  "private": true,
  "description": "SFSR-REMS shared UI — cross-app components for the Portal and Internal system.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./styles.css": "./src/styles.css"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@radix-ui/react-dialog": "^1.1.23",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "lucide-react": "^1.28.0",
    "next-themes": "^0.4.6",
    "tailwind-merge": "^3.6.0"
  },
  "peerDependencies": {
    "next": "^16.2.12",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "comment": "Depends on next/link and next/image. Both consumers are Next.js apps, so injecting them as props bought nothing and could not cross the Server/Client Component boundary."
}
