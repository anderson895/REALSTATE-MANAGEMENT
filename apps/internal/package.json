{
  "name": "@sfsr/internal",
  "version": "0.1.0",
  "private": true,
  "description": "SFSR-REMS Internal Management System — employee-facing. RUNS LOCALLY on an office LAN server, never deployed publicly.",
  "scripts": {
    "dev": "next dev --port 3001",
    "build": "next build",
    "start": "next start --port 3001 --hostname 0.0.0.0",
    "start:local": "next start --port 3001 --hostname 127.0.0.1",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hookform/resolvers": "^5.7.1",
    "@sfsr/domain": "*",
    "@sfsr/infrastructure": "*",
    "@sfsr/ui": "*",
    "@tanstack/react-table": "^8.21.3",
    "date-fns": "^4.4.0",
    "jspdf": "^4.2.1",
    "jspdf-autotable": "^5.0.8",
    "lucide-react": "^1.30.0",
    "next": "^16.2.12",
    "next-themes": "^0.4.6",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "react-hook-form": "^7.84.0",
    "recharts": "^3.10.1",
    "sonner": "^2.0.7",
    "tesseract.js": "^7.0.0",
    "zod": "^4.4.3",
    "zustand": "^5.0.14"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.3.3",
    "@types/node": "^26.1.2",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "postcss": "^8.5.25",
    "tailwindcss": "^4.3.3",
    "typescript": "^6.0.3"
  },
  "comment:devDependencies": "Same as the portal, and for the same reason — see the note there. This app is not deployed to Vercel, but a build run from apps/internal on a machine that installed only this workspace fails identically, and the two apps giving different answers to the same question is how one of them quietly stops building.",
  "engines": {
    "node": "22.x"
  },
  "comment:engines": "Matches the workspace root. Node 22 is required because firebase-admin -> jwks-rsa requires a runtime that can require() an ES module; see the note at the repository root."
}
